# Server Configuration
# Port for the daemon to listen on
PORT=3001

# Node Environment
# Options: development, production, test
NODE_ENV=development

# API Authentication
# Generate a secure API key with: openssl rand -base64 32
# This key must match the API key stored in the platform database for this server
# IMPORTANT: Keep this key secure and never commit it to version control
API_KEY=your-secure-api-key-here-generate-with-openssl

# SSL Configuration (optional, for HTTPS)
# Uncomment and configure these if you want to use HTTPS for daemon communication
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# Docker Configuration
# Docker socket path (usually default is fine)
# DOCKER_SOCKET=/var/run/docker.sock

# Logging
# Log level: error, warn, info, debug
# LOG_LEVEL=info

# Backup Configuration
# Directory where database backups will be stored
# Backups are organized by instance ID: {BACKUP_DIR}/{instanceId}/{backup-file}
# Ensure this directory has sufficient space and proper permissions
BACKUP_DIR=/var/lib/grumpy-wombat/backups
